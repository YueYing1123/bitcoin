# Round 9 Analysis

## 输入检查

- `master_dataset.csv` 行数：12,135
- 唯一 `doc_id`：12,135
- `data/raw/data-texts.json` 文本数：12,134
- 可关联原文的主表记录：12,134
- 缺原文记录：1

## 脚本

全量抽取脚本：

- `code/round-9-20260517-033120/build_master_dataset_dsv4.py`

脚本功能：

- 从 legacy SiliconFlow 脚本解析 `API_URL` 与 `API_KEY`，但不复制密钥。
- 读取当前 `docs/legacy/master_prompt.md` 作为提示词。
- 对每个可关联原文的 `doc_id` 调用 `deepseek-ai/DeepSeek-V4-Flash`。
- 正常输出追加保存为 JSONL。
- 失败写入本轮 failures JSONL。
- 每轮结束后将 JSONL 扁平化为 CSV。

