# round-2 数据构建资产整理计划

## 目标

遍历并理解 `D:\BaiduSyncdisk\Doctor\论文\数字货币\研究\数据构建` 中对新研究有用的代码、报告与数据，复制并整理到 `newstudy` 对应目录，为后续法学数据分析论文项目做准备。

## 边界

- 不移动、不删除原 `数据构建` 目录中的任何文件。
- 不把临时缓存、编译文件、压缩包和明显噪声资产作为正式研究资产迁入。
- 对既有结果只做整理和说明，不重新计算、不改口径。

## 分类口径

- `code/round-2-20260516-063920/`：可复用的抽取包、schema、prompt、工具代码。
- `code/legacy/`：历史根目录脚本，包括 Stata 分析脚本和辅助 Python 脚本。
- `data/raw/`：原始索引和文本 JSON/XLSX。
- `data/processed/extraction/`：模型抽取、正则抽取和最终 JSONL 结果。
- `data/processed/analysis_data/`：可直接进入统计分析的 CSV/DTA/JSON。
- `data/external/`：字段定义和 schema 依赖。
- `data/audit/`：模型原始运行、投票过程、测试样本等审计材料。
- `docs/legacy/`：旧 README、旧分析报告、提示词、草稿和说明材料。
- `result/round-2-20260516-063920/`：既有表格、图、日志、诊断和回归产物。

## 验收口径

- 代码、数据、报告、结果分别落入 `newstudy` 对应目录。
- 形成迁移清单和资产索引。
- 明确记录哪些文件最适合后续研究优先使用。
