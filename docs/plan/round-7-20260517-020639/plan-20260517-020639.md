# Round 7 Plan

## 任务

根据当前主数据集字段，追踪旧数据构建目录和 `newstudy` 中与 LLM 抽取有关的提示词、schema、字段配置、代码与数据底稿，确认此前数据集使用的提示词，并单独核查 `case_amount` 金额字段的提示词来源。

## 范围

- 检查 `data/processed/master/master_dataset.csv` 字段。
- 检查 `data/processed/extraction/final_all.jsonl` 中 LLM 抽取结果结构。
- 检查 `数据构建/ensemble_els/prompts/`、`docs/fields.yaml`、`ensemble_els` 抽取和投票代码。
- 不把正则匹配金额作为提示词来源。

## 产出

- `docs/legacy/master_prompt.md`
- 本轮分析与报告文件。

