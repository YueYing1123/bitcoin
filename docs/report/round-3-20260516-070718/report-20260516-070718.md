# round-3 主数据集合并报告

## 完成事项

- 已确认旧 `final_all_flat.csv` 不是完整主数据集。
- 已生成新的统一主数据集，合并 LLM 抽取、LLM 金额、正则金额、旧扁平分析数据、原始索引和文本审计信息。
- 已生成字段字典、审计文件、README 和带时间戳版本。
- 已将主数据集位置与使用约定写入 `MEMORY.md`。

## 主数据集位置

- `data/processed/master/master_dataset.csv`
- `data/processed/master/master_dataset.jsonl`
- `data/processed/master/master_dataset_dictionary.csv`
- `data/processed/master/master_dataset_audit.json`

## 验证结果

- 行数：12,135
- 列数：79
- `doc_id` 唯一，无重复
- `amount_master_cny` 非空：12,102
- LLM 顶层金额非空：11,684
- 全文正则金额非空：12,100
- 默认金额中 418 行使用全文正则最大值兜底

## 后续使用

后续研究默认读取 `data/processed/master/master_dataset.csv`。涉及金额的分析必须同时报告 `amount_master_source`；对金额敏感的回归或统计应使用 `amount_master_is_regex_fallback` 或 `amount_llm_regex_text_conflict` 做稳健性检查。
